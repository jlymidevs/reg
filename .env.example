VITE_SUPABASE_URL=YOUR_SUPABASE_URL
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Cloudflare Turnstile SITE key (public). Leave unset to disable the widget
# (the register Edge Function then skips verification too — rollout mode).
VITE_TURNSTILE_SITE_KEY=
